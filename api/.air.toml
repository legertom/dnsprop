# Air config for dnsprop API
# Docs: https://github.com/cosmtrek/air

root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/api ./cmd/server"
bin = "./tmp/api"
include_dir = ["cmd", "internal"]
include_ext = ["go", "mod", "sum"]
exclude_dir = ["tmp"]

[log]
time = true
